{% extends 'base.html.twig' %}

{% block title %}{{ event.name }}{% endblock %}

{% block stylesheets %}
    {{ parent() }}
{% endblock %}

{% block body %}
    <strong>{{ event.name }}</strong>
    <br>
    {{ event.description }}
    <br>
    <p>Créé par {{ event.referent.email }}</p>
    <br>
    <a href="{{ path('app_home') }}">{{ 'user.back.to.main.menu'|trans }}</a>
{% endblock %}
